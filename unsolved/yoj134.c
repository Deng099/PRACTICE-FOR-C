#include <stdio.h>
int main()
{
    int answer ;
    int times , count ;
    int guess , geshu , weizhi ;
    int done ; 
    scanf ("%d" ,&times );
    for ( answer = 1000 ; answer <10000 ;answer ++ )
    {

        for ( count = 1 ; count <= times ; count ++)
        {
            scanf ("%d%d%d" , &guess , &geshu , &weizhi) ;
            geshu = 
            weizhi = 
            if ()
            {
                done ++ ;
            }
        }
    }
    if ( done == times )
    {
        printf ("%d" , answer ) ;
    }
}